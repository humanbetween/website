"""HB Chief of Staff — generalist Telegram bot.

Composes hb_core primitives (TelegramDispatcher, ClaudeClient, NotionClient,
ValidationStore) into an ops bot that captures contacts, runs ad-hoc tasks,
and chats with HB context.

Entry point: `python -m chief.scripts.run_bot`.
"""

__version__ = "0.1.0"
